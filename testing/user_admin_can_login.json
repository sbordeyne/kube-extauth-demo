{
  "name": "user_admin_can_login",
  "description": "admin logs in and receives a bearer token",
  "steps": [
    {
      "request": {
        "method": "POST",
        "path": "/v1/login",
        "body": { "email": "admin@example.com", "password": "admin" }
      },
      "expect": {
        "status": 200,
        "jq": [".token != null and (.token | length) > 0", ".token_type == \"bearer\"", ".user.role.slug == \"admin\""]
      }
    }
  ]
}
