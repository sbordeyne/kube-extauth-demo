{
  "name": "user_reader_cannot_list_users",
  "description": "reader lacks unscoped users:read \u2014 listing users must be forbidden (expects 403)",
  "steps": [
    {
      "request": {
        "method": "POST",
        "path": "/v1/login",
        "body": {
          "email": "reader@example.com",
          "password": "reader"
        }
      },
      "expect": {
        "status": 200
      },
      "capture": {
        "token": ".token"
      }
    },
    {
      "request": {
        "method": "GET",
        "path": "/v1/users"
      },
      "expect": {
        "status": 403
      },
      "auth": "{{token}}"
    }
  ]
}
